# C# консольное приложение в Docker

## Чтобы запустить программу на своем компьютере (с установленным Docker), введите в терминал:
```bash
docker run --rm -it egorixa08/dockernis:latest
```

### P.S. Считает сумму двух чисел.
